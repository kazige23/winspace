"""Version string for the winspace package.

Kept in a tiny module so that build tooling, the CLI, and tests can all
import a single source of truth without pulling in heavier dependencies.
"""

__version__ = "0.1.0.dev0"

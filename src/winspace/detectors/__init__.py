"""Detector plugin point.

Each detector identifies one category of relocatable directory. See
:mod:`winspace.detectors.base` for the abstract contract and
:func:`winspace.detectors.base.discover_detectors` for auto-discovery.
"""
